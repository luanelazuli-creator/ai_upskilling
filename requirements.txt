# Core Framework
pydantic-ai==0.8.1

# Configuration
python-dotenv>=1.0.0
pydantic-settings>=2.1.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# CLI
typer>=0.9.0

# Utilities
colorama>=0.4.6
rich>=13.7.0

# OpenTelemetry Stack
# Pinado em 1.42.1 porque o chromadb (>=1.5) puxa
# opentelemetry-exporter-otlp-proto-grpc~=1.42, que exige sdk/api 1.42.1.
# api e sdk DEVEM ter a mesma minor (sdk 1.42 usa TraceFlags.RANDOM_TRACE_ID,
# ausente na api 1.39). semantic-conventions 0.63b1 acompanha o sdk 1.42.1.
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-semantic-conventions==0.63b1
# Exporter Jaeger (opcional; tracer.py degrada via try/except se ausente).
# O exporter thrift foi descontinuado no OTel novo — use OTLP se precisar.
# opentelemetry-exporter-otlp-proto-grpc==1.42.1

# Local Vector Store & RAG
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Data Processing & Conversion
pandas>=2.1.0
openpyxl>=3.10.0
python-docx>=0.8.0
pyyaml>=6.0            # frontmatter YAML (SPEC-004)
dateparser>=1.2.0     # detecção de janela temporal (SPEC-005)

# Memory & Persistence
sqlalchemy>=2.0.0     # SQLite metadata da EpisodicMemory (SPEC-003-2)

# PII Detection
transformers>=4.35.0
torch>=2.1.0

# Testing & Coverage
pytest-cov>=4.1.0
